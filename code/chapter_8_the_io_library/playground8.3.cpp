#include <iostream>
#include <string>

using std::cin; using std::cout; using std::endl;
using std::string;

int main()
{
  string line, word;
  while (getline(cin, line))
  {
    istringstream iss(line);
    iss >> line
    while (iss >> word)
    {
      
    }
  }
}